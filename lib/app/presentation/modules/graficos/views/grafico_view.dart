
import 'package:flutter/material.dart';

class GraficoView extends StatelessWidget {
  const GraficoView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Pagina de Gráficos'),);
  }
}